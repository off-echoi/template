import { css } from '@emotion/react';
import tw, { styled } from 'twin.macro';
import { Icon } from '@components/index';

const TwTest = () => {
  return <Icon index={10} />;
};

export default TwTest;
